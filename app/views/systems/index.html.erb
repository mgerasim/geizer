<h1>Очистительные системы</h1>

<table>
  <tr>
    <th><%= System.human_attribute_name(:name) %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @systems.each do |system| %>
  <tr>
    <td><%= system.name %></td>
    <td><%= link_to 'Показать', system %></td>
    <td><%= link_to 'Редактировать', edit_system_path(system) %></td>
    <td><%= link_to 'Удалить', system, confirm: 'Вы уверены?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новая очистительная система', new_system_path %>
